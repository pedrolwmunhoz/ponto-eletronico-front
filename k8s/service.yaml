apiVersion: v1
kind: Service
metadata:
  name: ponto-eletronico-frontend
  labels:
    app: ponto-eletronico-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: ponto-eletronico-frontend
